#include "main.h"
#include <stdlib.h>

/**
 * main - print the result of multiplying two numbers
 * @argc: argument count
 * @argv: argument array
 * Return: 0 (success)
 */
int main(int argc, char **argv)
{
	char *mul, *num1, *num2;

	if (argc != 3)
	{
		printf("Error");
		exit(98);
	}
	return (0);
}
